<div class="content no-bottom">
  <h1>Moxie TV episodes</h1>
  <p>Produced throughout 2006, MoxieTV was our attempt at making a weekly video for the theater. We didn't quite hit the stride we were hoping for, but we had fun along the way.</p>
</div>

<div class="content">

  <% @episodes.each_slice(2) do |episode| %>
    <div class="moxie-tv-row">
      <% (0..1).each_with_index do |n,i| %>
        <%= link_to episode[i], :class => "episode" do %>
          <%= image_tag "http://moxie.danchilton.com/pix/moxietv/#{episode[i].thumbnail}" %>
          <h4>
            <span><%= episode[i].title %></span>
            <small><%= episode[i].created_at.strftime("%b %-d, %Y") %></small>
          </h4>
        <% end %>
      <% end %>
    </div>
  <% end %>

</div>